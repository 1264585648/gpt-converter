# Build Validation

This file exists to keep the QA build-validation branch ahead of `main` while the pull-request-triggered GitHub Actions build verifies the current AuthAtlas production tree.

Remove this file before merging the validation branch.
